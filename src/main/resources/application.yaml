spring:
  application:
    name: Config-Server

  profiles:
    active: native   # ✅ VERY IMPORTANT

  cloud:
    config:
      server:
        git:
          uri: https://github.com/divyanjalee200178/Library-System-Configuration.git
          search-paths: platform,services
        native:
          search-locations: classpath:/configurations

server:
  port: 9000